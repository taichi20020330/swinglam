enum UploadType {
  GALLERY,
  CAMERA
}

enum uploadOpenMode {
  FROM_FEED,
  FROM_POST
}

enum feedOpenMode {
  FROM_FEED,
  FROM_PROFILE
}

enum PostMenu {
  EDIT,
  DELETE,
  SHARE
}

enum ProfileOpenMode {
  MYSELF,
  OTHER
}

enum WhoCareMode {
  LIKE,
  FOLLOW,
  FOLLOWINGS
}